# Conventionally, the src directory would be added here,
# and then the test directory.
# However, as supplied, the test directory does "add_subdirectory(src)",
# manually specifying the build location...

#add_subdirectory(src)
add_subdirectory(test)
